name: t21-eqtl
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # R version
  - r-base>=4.0

  # CRAN packages via conda
  - r-tidyverse
  - r-data.table
  - r-ggplot2
  - r-ggrepel
  - r-patchwork   # Panel composition (script 06)
  - r-arrow       # Reading parquet files (eQTL data)

  # Bioconductor packages via conda
  - bioconductor-deseq2
  - bioconductor-genomicranges  # Blacklist processing
